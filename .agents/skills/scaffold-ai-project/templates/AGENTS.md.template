# AGENTS.md — 代码代理协作规范

本文档是本仓库对所有参与协作的代码代理（AI Agent）的**统一入口**。目的是消灭幻觉报告、阻止夹带变更、保证 PR/commit 严格可复现。

## 一、元知识分层

| 层级 | 放什么 | 入口 |
| --- | --- | --- |
| **站立约束** | 跨任务始终成立的禁止项与必须项 | 本文件 + 下方「始终生效」硬约束 |
| **可复用流程** | 步骤清单与验证命令 | `.agents/skills/*/SKILL.md`（权威）；`.cursor/skills/` 兼容镜像 |
| **领域规范** | UI / 架构设计基准 | `docs/design.md` + `docs/architecture.md` |

> 会话或本地 Memory **不是**团队事实。反复踩坑须按规范沉淀为 Skill 并经人工审核。未经确认不得擅自新增 Skill。

---

## 二、始终生效（短硬约束）

1. **禁止夹带变更**：commit / PR 不得夹带任何无关改动。生产调参、整库 fmt、无说明的 `#[allow]`、跨模块顺手 refactor 一律违规；违规则使用 `git reset --mixed HEAD~1` 拆分。
2. **禁止幻觉代码**：
   - 有声明/定义必有实际调用；
   - 缓存字段必有对应的失效/存储策略；
   - 指标打点（Metrics）必须有正反双向统计；
   - 严禁空桩（Stub）伪装为已交付特性。
3. **Git Stash 安全**：stash 前必须 `git diff --stat`；stash 后必须 `cargo check --tests`；严禁 stash 构建脚本或锁文件。
4. **安全护栏**：
   - 密码/密钥等敏感字段在 DTO 序列化时必须显式忽略（如 `skip_serializing`）；
   - API Key 必须以安全哈希存储，列表与详情仅展示掩码（`sk-***`）；
   - **未经用户明确批准，绝对不得合并 main 分支或对外发版**。
5. **UI 设计规范**：所有前端界面改动必须遵守 `docs/design.md`；全局配置必须收敛到「设置」页，不随意新增并列顶级入口。
6. **前端 i18n 100% 对称**：所有用户可见文案必须走 `t('namespace.key')`，并同时写入 `locales/zh.ts` 与 `en.ts`；严禁硬编码单语言文案或仅靠 fallback。
7. **列表排序与检索**：所有实体列表的排序选项必须显式标明方向（如「延迟（低→高）」）；检索框 placeholder 必须说明支持的搜索字段。
8. **核心数据面与插件隔离**：业务定制、动态提取注入等功能必须优先作为插件（Plugin/Middleware）实现，严禁在核心数据面主干硬编码私有业务分支。

---

## 三、领域分层抽象原则

新增或重构模块时，前端与后端严格遵守以下四层边界：

1. **按领域分层**：
   - `api-types`：仅定义 API 契约与 TS 接口；
   - `resources`：负责具体的 HTTP Endpoint 与请求封装；
   - `domain-hooks`：负责加载状态、定时轮询、刷新与变更提交；
   - `pages`：仅负责页面布局、视图渲染与用户交互编排。
2. **优先领域语义**：使用具有明确业务语义的 Resource 函数（如 `listWorkers`、`evictCacheBlock`）；禁止建立动态万能 CRUD 或将不相关领域强行合并。
3. **验证闭环**：
   - 后端改动：必须通过 `cargo test --lib` 与相关集成测试；
   - 前端改动：必须通过 `npm run lint`、`npm run build` 和 `npm test -- --run`。
